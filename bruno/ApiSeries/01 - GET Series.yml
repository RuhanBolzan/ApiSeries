info:
  name: 01 - GET Series
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/api/series"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
