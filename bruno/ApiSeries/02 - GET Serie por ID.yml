info:
  name: 02 - GET Serie por ID
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/api/series/1"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
