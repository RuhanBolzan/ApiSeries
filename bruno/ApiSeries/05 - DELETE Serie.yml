info:
  name: 05 - DELETE Serie
  type: http
  seq: 6

http:
  method: DELETE
  url: "{{baseUrl}}/api/series/3"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
