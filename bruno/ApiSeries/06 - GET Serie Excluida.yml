info:
  name: 06 - GET Serie Excluida
  type: http
  seq: 7

http:
  method: GET
  url: "{{baseUrl}}/api/series/3"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
